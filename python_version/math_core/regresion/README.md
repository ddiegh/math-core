La regresion lineal (simple) nos ayuda a explicar de una forma simple datos, podemos describir y predecir comportamientos
Las formulas matematicas las calculamos de la siguiente forma (ocupando minimos cuadrados):
$$m = \frac{n\sum xy - \sum x \sum y}{n\sum x^2 - (\sum x)^2}$$

$$b = \bar{y} - m\bar{x}$$

En donde $m$ es la pendiente de la recta y $b$ la ordenada